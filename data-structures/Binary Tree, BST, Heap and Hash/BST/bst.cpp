#include <iostream>
#include <cstdlib>

#include "bst.h"

class BST {
  Node *root;

  VAL_T searchEntryR(Node *node, int *key) {
    if (node == NULL) {
      return -1;
    }
    if (node->entry.key
  }

  VAL_T searchEntryI(int *key) {
  }

  void insertEntry(int *key, VAL_T value) {
  }

  bool deleteEntry(int *key) {
  }

  void 
};
